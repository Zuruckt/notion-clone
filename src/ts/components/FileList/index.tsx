import React from 'react'
import { FileListProps } from '../../types/FileListProps'

function FileList({files} : FileListProps) {
    return (
        <>
            {
                files.map((file, key) => {
                    
                })
            }
        </>
    )
}